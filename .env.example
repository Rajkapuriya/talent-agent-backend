# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas

MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/talent-agent?retryWrites=true&w=majority


# OpenAI
OPENAI_API_KEY=
# Auth
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173


GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai/v1
GROQ_MODEL=openai/gpt-oss-20b

# Embeddings model (keep OpenAI key only if using OpenAI embeddings)
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_MODE=local
LOCAL_EMBEDDING_DIMS=1536
VECTOR_INDEX_NAME=candidate_profile_vector_index